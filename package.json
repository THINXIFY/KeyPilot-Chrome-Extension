{
  "name": "keypilot-password-generator",
  "version": "1.2.0",
  "private": true,
  "description": "KeyPilot — a premium, offline Chrome extension password security toolkit.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.js"
  }
}
